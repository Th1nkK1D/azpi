{
  "name": "azpi",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "src/index.ts",
  "scripts": {
    "prepare": "husky",
    "test": "bun test",
    "format": "oxfmt --write",
    "lint": "oxlint --fix",
    "lint:agent": "oxlint --fix -f agent",
    "check": "tsc --noEmit"
  },
  "dependencies": {
    "@agentclientprotocol/sdk": "^0.25.0",
    "@earendil-works/pi-coding-agent": "^0.80.2"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "husky": "^9.1.7",
    "lint-staged": "^16.4.0",
    "oxfmt": "^0.48.0",
    "oxlint": "^1.63.0",
    "typescript": "^5"
  },
  "lint-staged": {
    "*.{ts,tsx,js,jsx,mjs,cjs}": [
      "oxlint --fix"
    ],
    "*": [
      "oxfmt --write"
    ]
  }
}
